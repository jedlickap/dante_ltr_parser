python3 -m venv dante_ltr_parser
source dante_ltr_parser/bin/activate
pip --version
pip install --upgrade pip
pip install Bio
pip install pathlib
pip install argparse
pip freeze > requirements.txt
Rscript -e 'install.packages(c("dplyr", "ggplot2", "patchwork", "kableExtra", "webshot2"))'
sudo apt update
sudo apt install pandoc bedtools stretcher clustalw cat r-base-core
which stretcher 
sudo apt install pandoc bedtools clustalw r-base-core
Rscript --version
